import React from 'react';

import { Layout, Perfil as PerfilContainer } from 'containers';

function Perfil() {
    return (
        <Layout>
            <PerfilContainer />
        </Layout>
    );
}

export default Perfil;
